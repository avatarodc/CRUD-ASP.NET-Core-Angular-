/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateProduitDto } from '../../models/create-produit-dto';
import { ProduitDto } from '../../models/produit-dto';

export interface ApiProduitsPost$Json$Params {
      body?: CreateProduitDto
}

export function apiProduitsPost$Json(http: HttpClient, rootUrl: string, params?: ApiProduitsPost$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<ProduitDto>> {
  const rb = new RequestBuilder(rootUrl, apiProduitsPost$Json.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProduitDto>;
    })
  );
}

apiProduitsPost$Json.PATH = '/api/Produits';
