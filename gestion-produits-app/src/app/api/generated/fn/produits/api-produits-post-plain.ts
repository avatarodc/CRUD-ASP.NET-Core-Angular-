/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateProduitDto } from '../../models/create-produit-dto';
import { ProduitDto } from '../../models/produit-dto';

export interface ApiProduitsPost$Plain$Params {
      body?: CreateProduitDto
}

export function apiProduitsPost$Plain(http: HttpClient, rootUrl: string, params?: ApiProduitsPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<ProduitDto>> {
  const rb = new RequestBuilder(rootUrl, apiProduitsPost$Plain.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProduitDto>;
    })
  );
}

apiProduitsPost$Plain.PATH = '/api/Produits';
